# Mobile Legacy Package

This workspace is kept for backward compatibility with old Capacitor experiments.

Canonical mobile client:
- `android/` (Kotlin + Jetpack Compose)

Use root scripts for explicit legacy actions:
- `npm run mobile:legacy:web`
- `npm run mobile:legacy:apk`
