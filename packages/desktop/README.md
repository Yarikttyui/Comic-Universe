# Desktop Package

Canonical desktop entry point:
- `main.js` (Electron shell over web runtime)

Notes:
- `src/main` and `src/renderer` are legacy/prototype files and are not used by the current `start`/`build` scripts.
- Active scripts are defined in `packages/desktop/package.json`.
